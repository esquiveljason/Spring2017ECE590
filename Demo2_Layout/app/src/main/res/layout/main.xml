<?xml version="1.0" encoding="utf-8"?>
<!-- XML Extensible Markup Language. -->
<!-- This line define that this file is based on XML. -->

<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
	android:orientation="vertical" android:layout_width="fill_parent"	
	android:layout_height="fill_parent">
	<!-- Since it's a linear arrangement, android:orientation defines the contents will be laid vertically. -->
	<!-- fill_parent means fill all the screen width. -->

	<!-- Below is the content in the activity. -->
	<!-- id will determine the link to the button. -->
	<!-- wrap_content is used to define the height will just fix its text content. -->
	<!-- @string/button0 defines a text string, which is referred to strings.xml. Some times the direct Hardcoded string will bring some errors. -->
	
	<Button android:id="@+id/button0"
		android:layout_width="fill_parent"
		android:layout_height="wrap_content" 
		android:text="@string/button0" />	
	<Button android:id="@+id/button1"
		android:layout_width="fill_parent"
		android:layout_height="wrap_content"
		android:text="@string/button1" />
	<Button android:id="@+id/button2"
		android:layout_width="fill_parent"
		android:layout_height="wrap_content"
		android:text="@string/button2" />
	<Button android:id="@+id/button3"
		android:layout_width="fill_parent"
		android:layout_height="wrap_content"
		android:text="@string/button3" />
</LinearLayout>
